{
  "name": "cursor-goal",
  "private": true,
  "type": "module",
  "workspaces": ["packages/driver"],
  "scripts": {
    "build": "npm run build -w @cursor-goal/driver",
    "test": "npm test -w @cursor-goal/driver",
    "check": "npm run check -w @cursor-goal/driver",
    "install:global": "bash scripts/install-global.sh",
    "install:repo": "bash core/install.sh",
    "uninstall:global": "bash scripts/uninstall-global.sh",
    "package:dist": "bash scripts/package-dist.sh"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
