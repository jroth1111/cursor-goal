{
  "name": "cursor-goal",
  "private": true,
  "type": "module",
  "workspaces": ["packages/cursor-goal-runtime", "packages/driver"],
  "scripts": {
    "build": "npm run build -w @cursor-goal/runtime",
    "test": "npm test -w @cursor-goal/runtime",
    "check": "npm test && npm run check -w @cursor-goal/runtime && node scripts/verify-installed-parity.mjs && node scripts/verify-readme-claims.mjs && node scripts/verify-capability.mjs && node scripts/verify-governance-diff.mjs",
    "test:invariants": "npm run test:invariants -w @cursor-goal/runtime",
    "test:fast": "npm run test:invariants -w @cursor-goal/runtime",
    "sync:global": "npm run build && npm run install:global && node scripts/verify-installed-parity.mjs",
    "install:global": "bash scripts/install-global.sh",
    "install:repo": "bash core/install.sh",
    "uninstall:global": "bash scripts/uninstall-global.sh",
    "e2e:global": "bash scripts/e2e-global-cursor-agent.sh"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
