name: cursor-goal

on:
  push:
  pull_request:

jobs:
  standalone:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.19.0"
      - run: npm ci
      - run: npm run build
      - run: npm run check
      - run: npm test
